# 黑土寻味界面设计约定

## 设计方向

- 视觉类型：冰雪农业氛围型数据系统。
- 结构：E02 使用地图主导的 `map-diagram`，E01 使用工作台式数据可视化。
- 参考来源：用户提供的产销协同大屏截图；提取高密度信息层级、顶部指标带、中央地图和东北农业边缘元素，不复制其中错误的指标口径和行政地理。
- 主色：冻土深蓝、冰蓝、吉品绿、玉米暖黄。传统门店固定为冰蓝，第三空间固定为吉品绿，总需求固定为暖黄。

## 层级与排版

- E02 固定 1920×1080 设计画布并等比适配浏览器，核心阅读顺序为标题、指标带、中央供销地图、渠道占比与第三空间排行。
- 中文正文使用本地 Noto Sans SC Variable，标题使用本地 ZCOOL QingKe HuangYou，数字与英文标签使用本地 JetBrains Mono Variable。
- 面板使用低对比冰蓝半透明表面；背景的玉米、冰晶、雪花和黑土地只占边缘，中央数据阅读区保持低干扰。
- E01 沿用现有侧栏结构，通过同一颜色语义和字体系统增加趋势、排行、产能、库存、冻库和运输状态图。

## 数据表达规则

- 公斤、件、箱等需求单位永不相加，顶部和趋势图均按单位拆分。
- 预订单量表示园区需求，仅包含已确认与已完成记录。
- 两个环图只使用 B02 门店经营日报中的经营订单数和营业额。
- 未分类门店不进入正式总量，并在数据质量面板单独提示。
- 演示快照与真实数据整包切换；首次失败才加载演示快照，已有真实数据刷新失败时保留最后一次成功结果。

## 交互与动效

- E02 只保留周期、刷新、全屏、返回、图表展开和麦克风操作；没有文字提问入口。
- 动效仅用于数据刷新、地图线路、录音状态和图表浮层，并遵循 `prefers-reduced-motion`。
- 按钮、链接、输入框和选择框具有明确的键盘焦点、按下和禁用状态。

## 本地资源与部署

- ECharts、三套字体、Natural Earth Admin-1 东北三省 GeoJSON、吉品临时图标和东北背景全部随前端构建。
- 浏览器只请求同源 `/api/v1`；Cloudflare Worker 使用 `BACKEND_API_BASE_URL` 转发，并在服务端侧注入助手令牌。
- 生产 `dist` 禁止出现 localhost、在线地图、Google Fonts 或 CDN 地址。

## 2026-08-06 真实数据接入约定

- genre：`atmospheric`；macrostructure：E01 `workbench`、E02 `map-diagram`；theme：`studied-DNA`。
- 生产构建固定连接 `https://api.flexibility607.cn/api/v1`，不携带演示 JSON；演示快照仅由显式演示构建加入。
- 颜色、字体、间距、圆角、阴影和动效新增值统一从 `frontdesign-v1/tokens.css` 读取。旧样式中尚存的原始颜色属于既有视觉资产兼容例外，本轮新增样式不得继续增加散落色值。
- 数据截止时间、统计口径、单位、空态、错误态必须与图表同时展示；运输路线固定标注为经纬度估算路线。
- 登录令牌仅驻留内存，刷新凭证由安全 Cookie 保存；会话超时与跨标签退出状态同步。
- 信息架构不再展示已经下线的旧版整本导入入口；算法区统一承担拼车、拼仓、采购推荐与需求预测。
